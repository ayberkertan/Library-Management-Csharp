<UserControl x:Class="Kutuphane.App.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <StackPanel>
        <TextBox materialDesign:HintAssist.Hint="Kullanıcı adı"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                 Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,0,16" />
        <PasswordBox x:Name="PasswordBox"
                     materialDesign:HintAssist.Hint="Şifre"
                     Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                     Margin="0,0,0,8"
                     PasswordChanged="PasswordBox_OnPasswordChanged" />
        <TextBlock Text="{Binding ErrorMessage}" Foreground="#DC2626" TextWrapping="Wrap" Margin="0,0,0,12" />
        <Button Content="Giriş Yap"
                Command="{Binding LoginCommand}"
                Style="{StaticResource MaterialDesignRaisedButton}"
                Background="{StaticResource LibraryPrimaryBrush}"
                BorderBrush="{StaticResource LibraryPrimaryBrush}"
                Height="44" FontSize="15" />
        <TextBlock Text="Varsayılan: admin / 1234" FontSize="11" Foreground="#64748B"
                   HorizontalAlignment="Center" Margin="0,16,0,0" />
    </StackPanel>
</UserControl>
