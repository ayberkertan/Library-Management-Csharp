<Application x:Class="Kutuphane.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:vm="clr-namespace:Kutuphane.App.ViewModels"
             xmlns:views="clr-namespace:Kutuphane.App.Views"
             xmlns:conv="clr-namespace:Kutuphane.App.Converters"
             ShutdownMode="OnMainWindowClose">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <materialDesign:BundledTheme BaseTheme="Light"
                                             PrimaryColor="BlueGrey"
                                             SecondaryColor="Teal" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign3.Defaults.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <conv:BoolToStatusBrushConverter x:Key="BoolToBrushConverter" />
            <SolidColorBrush x:Key="LibraryPrimaryBrush" Color="#1B4965" />
            <SolidColorBrush x:Key="LibraryPrimaryLightBrush" Color="#62B6CB" />
            <SolidColorBrush x:Key="LibrarySecondaryBrush" Color="#BEE9E8" />
            <SolidColorBrush x:Key="LibrarySurfaceBrush" Color="#F8FAFC" />
            <SolidColorBrush x:Key="LibraryAccentBrush" Color="#CA8A04" />
            <SolidColorBrush x:Key="LibrarySidebarBrush" Color="#0F172A" />

            <Style x:Key="SidebarButton" TargetType="Button">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Foreground" Value="White" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="HorizontalAlignment" Value="Stretch" />
                <Setter Property="HorizontalContentAlignment" Value="Left" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="Padding" Value="16,14" />
                <Setter Property="Margin" Value="8,4" />
                <Setter Property="FontSize" Value="15" />
                <Setter Property="FontWeight" Value="Normal" />
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="{TemplateBinding Background}"
                                    CornerRadius="8" Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                  RecognizesAccessKey="True"
                                                  TextElement.Foreground="{TemplateBinding Foreground}" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#1E3A5F" />
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#2D4A6F" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <DataTemplate DataType="{x:Type vm:UsersViewModel}">
                <views:UsersView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:BooksViewModel}">
                <views:BooksView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:LendViewModel}">
                <views:LendView />
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:ReturnViewModel}">
                <views:ReturnView />
            </DataTemplate>
        </ResourceDictionary>
    </Application.Resources>
</Application>
